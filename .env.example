# Bootstrap env loaded first.
# Copy this file to `.env`, then copy the feature-specific example files you need.
#
# Load order:
# 1. .env
# 2. .env.discord
# 3. .env.swg-chat
# 4. .env.ent-bot
# 5. .env.web-api
# 6. .env.status
# 7. .env.launcher
# 8. .env.database

# Allowed values: live, dev, tc
BOT_MODE=live
